import { RainbowDirectiveDirective } from './rainbow-directive.directive';

describe('RainbowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
